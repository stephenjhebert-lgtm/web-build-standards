# Custom code discipline

Keeping the local `webflow_head_paste.html` in sync with Webflow Site Settings → Custom Code → Head, and when to push CSS there instead of fighting `style_tool`.

**Status:** stub — content lives in `mcp-field-guide.md` Part 2.4 and Part 6.2. This file will be split out when that material grows beyond ~50 lines.
