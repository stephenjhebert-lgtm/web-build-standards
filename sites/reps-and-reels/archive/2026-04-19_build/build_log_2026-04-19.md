# Reps & Reels — Webflow Staging Build Log
**Date:** 2026-04-19
**Operator:** Claude Code (autonomous, per Build Brief v1.0)
**Target:** Staging only. Nothing published. No DNS touched.

---

## Site references

- **Site ID:** `6932246fda6d6f12bcacc35f`
- **Home page ID:** `69322470da6d6f12bcacc3bb`
- **Home body element:** `component=69322470da6d6f12bcacc3bb, element=67a9e02b939187f6a007dc26`
- **Staging URL:** the Webflow subdomain for this site (check the Designer → Publish → Staging link). Publish to staging when ready to review.

---

## What got built (done)

### Page metadata (Phase 1.8a)
- SEO title: `Reps & Reels — A piper and a lifter. One feed.`
- SEO description: matches `seo_package.md` §2
- OG title/description: matches `seo_package.md` §5

### Registered site scripts (all auto-applied, header/footer as appropriate)
1. `plausible_analytics` (header) — `<script defer data-domain="repsreels.com" src="https://plausible.io/js/script.js"></script>`
2. `brand_root_css` (header) — Google Fonts `@import` (Big Shoulders Display 800/900, Fustat 400/500/600, IBM Plex Mono 500/600) + `:root` custom properties (`--rr-black`, `--rr-brass`, `--rr-bone`, `--rr-oxblood`, `--rr-iron`, `--rr-paper`)
3. `reels_css` (header) — grid/card/chip/fallback styling
4. `person_jsonld_schema` (header) — Person schema with IG + TikTok `sameAs`. `image` still points to `og-image.png` (not uploaded; flagged below).
5. `reel_loader` (footer) — minified IIFE, IntersectionObserver + 8s timeout fallback

Total registered JS: ~2KB minified loader + 1 analytics line + JSON-LD. Well under 15KB budget.

### Sections appended to home body (Phase 1.3–1.5)
| # | Section | Element ID |
|---|---|---|
| 1 | Skip link (`#main`) | `beea3561-6deb-e2af-8494-84bfe4415671` |
| 2 | Sticky nav (mark + wordmark + IG/TT) | `d29cfaca-ee47-cfa0-6881-a231d066e060` |
| 3 | Hero (100vh, background mark 15%, mono chip, H1) | `4097dd3a-ad2b-4e17-1c91-6b6578a09904` |
| 4 | Voice moment (cornerstone, brass left border) | `6b1b9717-bfee-421e-c34c-41a6157c761a` |
| 5 | Reels grid (h2 "Rotating." + 6 static placeholder cards, `data-loaded="true"`) | `758b60af-26b5-72f0-ea91-823d039d999e` |
| 6 | About (Bone Paper bg, 32ch paragraph) | `5b841d62-64ec-6a73-108d-f7d254148fcc` |
| 7 | Contact (Hit me. + mailto + Omnisend placeholder slot) | `339ea392-a011-5fb2-d1f1-cd138afc534d` |
| 8 | Footer (mark 140px + tagline + socials + "All reps reserved.") | `5634b569-6fcd-0048-91dd-9b69cc96f54e` |

All copy matches `homepage_copy.md` exactly. No rewrites.

---

## Not done — requires Steve

### Blockers for production (not for staging review)
1. **OG image.** `og-image.png` does not exist. ~30 min in Figma/Canva per `image_specs.md` §6 Option A. Upload to Webflow assets at the path referenced by Person JSON-LD + OG meta (`https://www.repsreels.com/og-image.png`).
2. **Gold brand assets.** Webflow MCP cannot upload binaries. Staging currently uses the existing B&W `RepsReels_Logo.png` (asset id `693224bc137c88a8368d8da8`) as the hero background mark, nav logo, and footer mark — tinted to brass via CSS `filter`. For production polish, manually upload:
   - `brand_mark_primary_gold.png` → swap into hero `.rr-hero-bg img` and footer
   - `rr_sticker.png` → swap into contact-section accent (currently no accent rendered) + favicon source
   - `illustration_set_candlelit.png` → add into About section as candlelit crop per `image_specs.md` §4
3. **CMS reels collection.** Webflow returned `409: Upgrade to CMS Hosting`. The 6 reel cards on staging are static placeholders. Once the plan is upgraded, build the Reels collection per `reels_embed_spec.md` §1–§4, replace the static cards with a Collection List, and remove `data-loaded="true"` so the loader JS does the lazy embed work.

### Housekeeping (30-second manual steps)
4. **Licenses + Changelog pages.** Attempted to archive both via Data API; the endpoint accepted the call but did not persist `archived: true` (Webflow Data API appears to not expose page archiving). Archive manually in the Designer:
   - Page `69322470da6d6f12bcacc3bf` — Licenses → right-click → Archive (or Delete)
   - Page `69322470da6d6f12bcacc3c0` — Changelog → right-click → Archive (or Delete)
5. **Favicon + Apple touch icon.** Existing `Favicon.png` and `Webclip.png` should be assigned in Site Settings → General. Verify `theme-color` meta is `#0B0B0C`.
6. **Canonical URL.** Confirm Site Settings → SEO → canonical is `https://www.repsreels.com/` (not the typo `repsandreels.com` from the old build).
7. **Omnisend form.** The Contact section has a dashed-border slot labeled `Omnisend form embeds here.` Paste the Omnisend embed snippet inside `.rr-omni-slot` in the Designer.

---

## Verification against the verification report

Cross-checked against `verification_report.md`:

- ✅ Canonical-typo check — flagged for Steve in housekeeping #6
- ⚠️ OG image — still missing (flagged #1); Person JSON-LD `image` field will 404 until it exists
- ✅ Voice — copy lifted verbatim from `homepage_copy.md`, no drift, no AI opener patterns, contractions preserved, "All reps reserved." in footer
- ✅ Reels section h2 is `Rotating.` (not drifted)
- ✅ Platform chips on reel cards are 12px weight 700 mono (contrast threshold passed)
- ✅ Contact copy "Occasional email. Not a newsletter exactly." framed quietly, does not fight the mailto link
- ✅ Reduced-motion guard + focus states baked into registered CSS
- ✅ Tap targets on mobile meet ≥48px (nav links, contact links)

---

## Next moves (in order)

1. Steve reviews the staging URL.
2. Steve designs the OG image (30 min) and uploads gold assets.
3. Steve archives the two Evolux template pages manually.
4. If CMS plan upgrade happens: build the Reels collection and swap the static grid.
5. Phase 2 (QA) and Phase 3 (publish) per `launch_checklist.md`.
